const util = require('util');
const fs = require('fs');
// generate unique id
const { v4: uuidv4 } = require('uuid');

// read - readfilesync
// write - writefilesync
// get
// add
// remove

module.exports